# scalp_ping_5s_live "MAX" override (2026-02-12 JST)
# Intent: increase pace + sizing envelope after exit unstick + TP reject fix.

# Pace
SCALP_PING_5S_ENTRY_COOLDOWN_SEC=0.10
SCALP_PING_5S_MIN_ORDER_SPACING_SEC=0.05
SCALP_PING_5S_MAX_ORDERS_PER_MINUTE=84

# Capacity (note: may be overridden by systemd unit tail overrides)
SCALP_PING_5S_MAX_ACTIVE_TRADES=40
SCALP_PING_5S_MAX_PER_DIRECTION=24

# Sizing
SCALP_PING_5S_MIN_UNITS=300
SCALP_PING_5S_BASE_ENTRY_UNITS=8000
SCALP_PING_5S_MAX_UNITS=22000

# Reduce protection-related order rejects (TAKE_PROFIT_ON_FILL_LOSS / LOSING_TAKE_PROFIT)
# by allowing an additional widened-fallback submit attempt.
ORDER_SUBMIT_MAX_ATTEMPTS=3
ORDER_PROTECTION_FALLBACK_MAX_RETRIES=2

# Allow margin usage up to ~90% (ops target 85-92%)
SCALP_PING_5S_MIN_FREE_MARGIN_RATIO=0.10

# Per-worker risk guard knobs (loaded via EnvironmentFile for this service only)
MAX_MARGIN_USAGE=0.90
MAX_MARGIN_USAGE_HARD=0.92
FREE_MARGIN_SOFT_RATIO=0.14
FREE_MARGIN_HARD_RATIO=0.10
