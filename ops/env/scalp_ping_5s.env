# Optional local override env for quant-scalp-ping-5s.service
# Present for compatibility with EnvironmentFile reference in service unit.

# 5秒スキャの確率スケール適用後、最小ユニット判定を現状運用に合わせて引き下げる
ORDER_MIN_UNITS_SCALP_FAST=100
