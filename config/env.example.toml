gcp_location = "asia-northeast1"
gcp_project_id = "your-project-id"
gcp_pubsub_topic = "quant-topic"
news_bucket_name = "quantrabbit-fx-news"

oanda_account_id = "000-000-00000000-000"
oanda_token = "replace-with-your-token"
oanda_practice = "true"  # "true" for practice, "false" for live

openai_api_key = "sk-test-placeholder"
openai_model = "gpt-5-mini"
openai_decider_model = "gpt-5-mini"
openai_exit_model = "gpt-5"
openai_news_model = "gpt-5-nano"
openai_planner_model = "gpt-5-mini"
openai_max_month_tokens = "600000"
openai_max_month_usd = "60"
openai_cost_per_million_input = "0.25"          # USD per 1M input tokens (GPT-5 mini)
openai_cost_per_million_output = "2.00"         # USD per 1M output tokens
openai_decider_cost_per_million_input = "0.25"
openai_decider_cost_per_million_output = "2.00"
openai_exit_cost_per_million_input = "1.25"     # GPT-5 (standard)
openai_exit_cost_per_million_output = "10.00"
openai_exit_max_month_usd = "120"
openai_exit_max_month_tokens = "400000"
daily_target_pct = "0.10"           # 1日あたりの目標リターン（10%）
trades_per_day = "10"               # 1日の想定トレード回数
risk_share_of_target = "0.5"        # 目標達成のため1トレードに割り振る比率（50%）

# --- Soft gating params (optional; trader_service reads from env) ---
SOFT_COOLDOWN_MIN = "60"            # 直近エントリからのソフトクールダウン（分）
SOFT_COOLDOWN_FACTOR = "0.25"       # クールダウン中のロット係数
EVENT_SIZE_FACTOR = "0.15"          # 指標前後（±60分想定）のロット係数
SPREAD_SOFT_MIN = "1.0"             # このpips超過でロット逓減開始
SPREAD_HARD_MAX = "3.0"             # このpips以上はロット0（実質禁止）
DAILY_SOFT_STOP_PCT = "-0.01"       # 当日損益（NAV比）<= -1%でソフトストップ
DAILY_SOFT_FACTOR = "0.10"          # ソフトストップ時のロット係数
EXIT_GPT_TRIGGER_PIPS = "8.0"       # 含み益がこのpips以上でGPT exit助言
EXIT_GPT_REFRESH_SEC = "180"        # 同一ポジションに対する助言の最短間隔
EXIT_GPT_CLOSE_CONF = "0.55"        # 成行クローズ採用の信頼度しきい値
EXIT_GPT_ADJUST_CONF = "0.35"       # SL/TP調整採用の信頼度しきい値
