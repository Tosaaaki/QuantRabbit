# 弱点台帳 + 構造利益化の設計図 (2026-07-18, Codex実行用)

`design_profit_path_4x_20260718.md` (レーンA-C) と `design_profit_structure_repair_20260718.md`
(欠陥1-5, S1-S6) を包含する完全版の弱点台帳。番号 W1-W20。
各弱点に修理方向と担当を付す。不変条件 (fail-closed / shadow-only / AI_ORDER_AUTHORITY=NONE /
live昇格はオペレーター明示承認) は全修理に適用。

## A. シグナル/アルファの弱点

- **W1 出口が非対称でない** (=旧欠陥3): 入口は構造判定、出口は固定倍率TP/SLのみ。勝ち玉の右尾を切断。
  → structure-break exit arm family V3 (freeze時にclosed candleから**事前確定**したswing levelを封印、
  時間内はそのlevel割れ/戻り超えまで保有。TP無し・time-stop併用)。既存182候補と identity 分離。
- **W2 指標セット固定・チューニング語彙が粗い** (=旧欠陥W2): GO/CAUTION/STOP はペア単位のみ。
  regime→family選択圧をかけられない。→ `QR_AI_REGIME_SUPERVISION_V2`: ペア×候補family単位の
  GO/CAUTION/STOP + regime宣言 (TREND/RANGE/SQUEEZE/EVENT) を追加。ボットは宣言regimeに
  不適合のfamilyを自動CAUTION化。
- **W3 エントリーが足確定境界のみ**: 決定は4h/closed-M1境界。オペレーターはブレイク/リテストの
  現在進行イベントで入っていた。→ M5内のtick非依存な決定論トリガー (直近closed swing level への
  touch-and-reject を次のS5 openで判定) を fast bot admission に追加。lookaheadなし。
- **W4 イベント/ニュース盲目**: news pipeline は trader consumer-only。ボットは経済指標カレンダーを
  見ない。→ pre-entry event gate: 高インパクト指標の前後N分は新規admission停止 (これは既存
  calendar データで実装可)。イベント後モメンタムはW2のEVENT regime familyとして別途候補化。
- **W5 セッション構造の不在**: Tokyo/London/NY の挙動差が階層に無い (SITUATION_WEIGHTS の思想が
  botに未移植)。→ session を candidate family の第一級次元にする (同一ロジック×session別 GO)。
- **W6 レンジ回転レーンの不在**: 歴史的に利益源だった range/SQUEEZE 回転 (LIMIT受動往復) が
  bot撲滅時に消えたまま。FX時間の過半はレンジ。→ レーンF: range-rail 回転 family
  (レール事前確定・受動LIMIT・両側、S5 exact評価)。`range_rail_geometry_repair` の既存成果を流用。
- **W7 オペレーター実績の未信号化** (=旧欠陥4): レーンE (precedent replication) で対応。

## B. 執行の弱点

- **W8 受動LIMIT+TTL90秒の逆選択**: fillするのは価格が戻って来た時だけ=不利な状況で選択的に
  約定し、トレンドでは不成立。→ momentum文脈 (W2のTREND宣言時) は STOP entry arm を主用、
  受動LIMITはRANGE宣言時に限定。4分位受動armのpaired比較で逆選択コストを定量化して封印。
- **W9 部分利確/分割が無い**: 全量in/全量out。スキャルプ回転の実績と乖離。→ V3 family に
  half-at-1R + runner (structure-break) の2段出口armを追加。
- **W10 スプレッド時間帯を回避しない**: コストゲートは個別拒否のみで、スケジューラは
  rollover (21-22UTC) や金曜後半に平然と決定を出し続ける。→ 低コスト時間帯マスクを
  admission に追加 (実測spread分布から事前宣言)。
- **W11 スリッページ/約定拒否の実測データ皆無** (=旧欠陥5と連動): shadow-onlyの限り永遠に
  取れない。→ T1 micro-live (下記W16) が唯一の解。

## C. ポートフォリオ/リスクの弱点

- **W12 相関盲目**: 28ペアを独立扱い。USDテーマで20ペア同時動作時の実効リスクが
  per-pair capの数倍になる。逆に「テーマ一致3ペア同時攻め」(実績あり) も表現不能。
  → 通貨エクスポージャ集計 (8通貨netポジション) を admission の一級制約に。
  同一テーマの意図的バスケットは conviction ladder の客観条件として定義。
- **W13 pips単位の選抜歪み**: scorecard集計がpips建てでペア間のpip価値差を無視 (NAV%原則違反)。
  → 全scorecardに quote-currency換算のNAV%列を並記し、選抜キーをNAV%側へ移行。
- **W14 equity-curve制御が無い**: 日次損失停止・連敗縮小・好調時の逓増が未実装。
  → conviction ladder (S4) に equity-curve条項を統合: 日次 -3% NAV 停止、
  3連敗でサイズ半減、週次+5%超で次週ベース+25% (全て事前宣言)。

## D. 学習ループの弱点

- **W15 適応タイムスケールが日〜週**: 6hチューニング+新cohort要件で、レジーム変化への追従が
  構造的に遅い。→ W2のfamily粒度GO/STOPは**既存証拠の再利用でなく配分変更**なので
  即時適用可と契約に明記 (新エッジ主張には使えない)。
- **W16 中間資本ティアの欠落** (=旧欠陥5): T0 shadow → T1 micro-live (NAV2% risk pool、
  執行データ収集目的、オペレーター承認必須) → T2 scaled。昇格契約モジュールを実装する。
- **W17 監督自身が採点されない**: GO/CAUTION/STOPの的中率を誰も測っていない。
  → supervision outcome ledger: 各監督行を24h後の実現ボラ/方向と突合し、
  supervisor精度をmonthly封印。精度が閾値未満のfamilyは監督を自動CAUTION化。
- **W18 棄却候補の死因分類が無い**: permanent shadowは結果を貯めるが「なぜ死んだか」の
  taxonomy が無く、次のfamily設計に還流しない。→ 棄却時に死因コード
  (COST/DIRECTION/EXIT_TIMING/REGIME_MISMATCH/...) を必須化し、四半期でtaxonomy集計。

## E. 運用/メタの弱点

- **W19 契約肥大とゲート負債**: incidentごとにゲートが増え、退役プロセスが無い。714行の契約は
  それ自体がスループットリスク (=欠陥2の根本原因)。→ 四半期 gate-debt review:
  全ゲートに (防いだ損失の実績 / 殺したエントリー数) を記録し、比率最低のゲートを
  統合・退役候補としてオペレーターに提示。
- **W20 直列の将来テストで証明速度が遅い**: 2週間に1本のprospective testでは PROVEN 行が
  増えない。→ 並列事前登録: 複数候補それぞれに独立の将来窓lockを許可 (family横断のWRC分母は
  登録簿で管理)。登録簿 `research/data/prospective_registry_v1.json` を新設。

## 実装状況 (2026-07-18 Claude、テスト付き・全suite green)

| 弱点 | モジュール | 状態 |
|---|---|---|
| W16 | `micro_live_promotion_contract.py` | 実装済み (T1契約+承認束縛検証) |
| S1/欠陥1 | `close_distance_gate.py` | 実装済み (pre-entry close-distance + max_admissible_hold) |
| W12 | `currency_exposure_guard.py` | 実装済み (8通貨net NAV%キャップ、ヘッジ相殺対応) |
| W2/W8/W15 | `regime_supervision_v2.py` | 実装済み (regime宣言×family行、不整合GO自動降格、6h TTL→UNSUPERVISED) |
| S2/欠陥2 | `gate_throughput_slo.py` | 実装済み (ファネル封印、floor割れでkiller gate命名+P0フラグ) |
| W14/S4 | `conviction_ladder.py` | 実装済み (0.25%基底、条件数で2x/4x、連敗半減、日次-3%停止) |
| W20 | `prospective_registry.py` | 実装済み (hash鎖・事前登録・family分母・成熟前評価拒否) |
| W17 | `supervision_outcome_scorer.py` | 実装済み (GO/CAUTION/STOP採点、精度floor割れfamilyの自動CAUTION指名) |
| W18 | `rejection_taxonomy.py` | 実装済み (固定死因コード必須化+family別集計) |
| W1/W9 | `asymmetric_exit_shadow.py` | 実装済み (TP無しstructure-break exitの独立shadow評価器。封印カタログ非接触のpaired再採点) |
| W13 | `nav_normalization.py` | 実装済み (pips→NAV%変換、明示レート必須) |
| W10 | `cost_window_mask.py` | 実装済み (事前宣言UTC高コスト窓マスク、デフォルトrollover 21-22) |

追加完了 (2026-07-18 第2弾):
- **未来テストexecutor実装済み**: `final-test` subcommand + `evaluate_locked_final_test`。
  8/3成熟前の実行・研究manifestの再利用・束縛改竄を全拒否。事前宣言判定
  (stressed net>0 && stressed PF>=1.05) 封印済み。残るのは壁時計のみ。
- **shadow層のruntime配線完了**: `build_fast_bot_shadow_admission_binding_v1` が
  close-distance / cost-window / 通貨エクスポージャの3ゲート判定を毎サイクル封印し、
  将来GO契約の必須提示物 (`future_go_contract_must_bind_this_artifact=true`) にbind。

コードでは完成させられない残余 (時間・証拠・境界に依存し、先回りは契約違反):
- レーンA未来テストの**実行** (8/3以降の新規取得後、コマンド1回)
- live trader runtime (本体repo・ルーチン所有) への同ゲート接続 = Codex S1-S6
- W1/W9のカタログ昇格 = shadow paired証拠が正になった時のみ (証拠先行の禁止は不変条件)

## 証明の階段 (narrative-first discipline、2026-07-18 オペレーター方針で明文化)

「過去ローソクをナラティブに分析して、リプレイ検証する」を全familyの必須手順にする。
ただし各段が証明できる範囲を混同しない:

1. **ナラティブ**: 過去ローソクから「誰がなぜ負けて、その反対側をどう取るか」の機構仮説を書く。
   機構なきパターンは登録不可。(例: 週末クローズ跨ぎの流動性欠落が exit を滑らせる →
   事前に跨がなければ損失族が消える)
2. **因果ルール化**: ナラティブを entry 前情報だけで計算できる規則に変換する。
3. **TRAINリプレイ**: 規則がナラティブの予測どおり動くかを exact bid/ask + receipt で検証。
   ここで「効く形」を1つに固定する (grid事前宣言・選抜はTRAINのみ)。
4. **外部窓リプレイ (VALIDATION)**: ナラティブ形成に使っていない窓で無変更複製。
   予測が再現しなければナラティブ棄却 (死因コード必須)。
5. **未来窓**: 戦略凍結時に存在しなかったデータでの1回評価。**ここで初めて「証明」と呼ぶ**。
   1-4は「反証されなかった」まで。今セッションの偽+689.4pは、精密リプレイが
   見えない仮定1つ (execution-gap事後削除) で崩れる実例。

実績: この階段を通った例が既に2つ — 週末跨ぎナラティブ (TRAIN反転→VAL +969.7p複製、8/3判定待ち)、
出血日ナラティブ「負け込んだ日に入り続ける取引は負の期待値」(50p日中ストップ、VAL最悪日-110p改善+net+29%)。

## 不参加は改善ではない (2026-07-18 オペレーター原則)

「トレードしなければ損失が出ない」型の改善は前進しない理論として**禁止**する。
スロットル (SKIP/HALF_SIZE/day-skip) は損害上限の道具であって、エッジの改善に数えない。
負けた日は入り口と出口が悪かった日であり、義務は診断と修理:

1. 負の日ごとに entry/exit fingerprint を封印する (`run-losing-day-entry-exit-diagnosis.py`)。
2. fingerprint から修理仮説 (ナラティブ) を作り、証明の階段に載せる。
3. スロットル系の採否判定には機会損失の実測 (抑制した取引の合計pips) を必須とし、
   正なら格下げ。day-skip 1p は VALIDATION で +1,658.5p を逃して棄却済み (実例)。

既知の出口証拠 (封印済みTRAIN): hold 6h化 net -185.9 / cadence 1h化 -712.4 / rank1化 +403.8。
=> この時間軸の負け日は「出口が遅い」問題ではない。エントリー品質 (シグナル強度・時刻・regime) の
診断が主戦場。

## 時計ヒューリスティック禁止 (2026-07-18 オペレーター原則)

時刻・曜日で分けるのは人間の代理変数であり禁止。時刻が「悪く見える」のは背後の状態
(スプレッド水準・シグナル減衰・流動性) がその時刻に多いだけ。機械は状態そのものを測る:

- エントリー条件付けは決定時点で測れる状態変数のみ: |score| (シグナル強度)、
  spread/|score| (コスト対シグナル比)、regime宣言。時刻armは診断表示のみで採用不可。
- ある状態で現手法が負けるなら、答えは休みでも時刻除外でもなく**その状態で勝つ別family**
  (薄商い状態=スプレッドを取る側の受動レーンC、レンジ状態=レールF)。
  family切替は `regime_supervision_v2` が既に実装済み。
- 実装: `run-state-conditional-entry-rehearsal.py` (S1 |score|床 / S2 コスト比 / S3 両方、
  時刻不使用、combined-fingerprint起源のため未来窓が最終判定)。

## W21 全天候要件 (2026-07-18 オペレーター指示で追加)

「レンジでもトレンドでも高低ボラ両方でとってこれないといけない」= regime×vol の 2×2 全セルに
検収済みfamilyを持つこと。実測の現在地:

| セル | 担当family | 状態 |
|---|---|---|
| トレンド×高vol | S5 survivor (RETURN_PIPS DIRECT 8h/12h) | VALIDATION複製済み・未証明 (8/3判定) |
| トレンド×低vol | 同上でカバー可能か要day分解 | 未測定 (day-regime分解が次の実験) |
| レンジ×(高/低)vol | **既存192では構造的に不可能**: INVERSE 96本全て TRAIN負 (最良-891.5p)。
  4-24h軸の向き反転はレンジ捕捉にならない | レーンF (短周期レール回転・受動LIMIT両側・
  range_rail_geometry_repair流用) を新規設計。M5取得後にTRAIN/VAL/WRC |
| イベント×高vol | W2のEVENT regime + W4 event gate (回避が先、捕捉はその後) | ゲート実装済み・捕捉family未設計 |

規律: セルごとに独立family・独立検収。1つのfamilyで全セルを取ろうとしない
(それが「何をやっても利益が出ない」単一エンジン設計の再演)。regime判定は
`regime_supervision_v2` の宣言regime + 因果的day分解で行い、セル間の資本配分は
family粒度GO/CAUTIONで切り替える。

## W22 ポジション運用の全武装 (2026-07-18 オペレーター指示「手段を選ばず」)

検証対象として全て階段に載せる。事前の力学予想も宣言しておく:

| 手段 | 機構 | 予想される適性 | 検証手段 |
|---|---|---|---|
| 集中サイジング (配分) | サイズ∝確信度 | 全レーン (オペレーター2025実証) | `run-conviction-sizing-rehearsal.py` (実行中) |
| ピラミッティング | 順行時に増し玉 | モメンタム系 (エッジと同方向のサイズ分散) | `addon_ladder_shadow` (S5 open経路で実測) |
| ナンピン | 逆行時に増し玉 | レンジ系のみ。モメンタム系ではworst day増幅 | 同上。**無制限ナンピン (マーチンゲール) は禁止**: 追加回数・総エクスポージャ上限を事前宣言 |
| 短期×長期の同時保有 | 別ホライズンのシグナル同時運用 | 12h survivor + 分オーダー fast bot。同一シグナルの短縮版は不可 (6h化 -185.9で実証済み) | レーンC成熟後にポートフォリオ合成をリプレイ |
| 両建てヘッジ | 長期テーゼ維持+短期逆回転 (OANDA追加マージン0) | レジーム転換期 | 同上 |

規律: どの手段も「損失を隠す」方向の使い方 (無制限ナンピン、含み損の未計上) は
6月の出口リーク事件の再演として禁止。増し玉系は max_adds と総エクスポージャ上限を
契約値として封印してから検証する。

## W23 テクニカルの組み合わせと独自指標 (2026-07-18 オペレーター指示)

現状の正直な評価: **組み合わせは出来ていない**。
- S5 survivor は単一因子 (8h リターンの断面ランク) + 週末ゲートのみ。
- fast bot は MTF階層 (M1..D) の固定組み合わせで、状況適応しない。
- 相互作用特徴 (モメンタム×vol状態、モメンタム×レンジ内位置)、出来高、構造 (swing)、
  クロスアセットは研究レーンに未投入。全天候はここで作られる。

### 独自テクニカル (このシステムだけが持つデータからの指標)

1. **執行可能スプレッド状態**: exact bid/ask S5 から作る流動性パルス (retail指標はmid建てで作れない)。
   spread正規化モメンタム (S2 arm) はその第一号。
2. **8通貨strength分解**: 28ペアのleast-squaresから作る通貨強度index (レーンB本体)。
3. **メタ・モメンタム**: 自前の outcome ledger から「どのfamilyが今効いているか」を指標化
   (W17監督採点の副産物)。市場でなく自分の成績のregimeを読む。
4. **オペレーター先例指標**: 2025年勝ちトレードの状況タプル一致度 (レーンE)。

### 組み合わせ探索の規律 (蜃気楼防止)

- 相互作用を含む組み合わせ空間は指数爆発する。**34日のS5では検証不能**
  (192候補で既にHolm p=1.0。候補を増やすほど有意性は消える)。
- よって組み合わせ研究の主戦場は **2020-2026 M5 (レーンB取得後)**: 年単位のTRAIN/VAL/TESTと
  week block bootstrap + WRC で、相互作用 family を事前宣言の小さな束 (≤24) ずつ検証する。
- 「予測の的中」の定義も封印する: 的中率でなく **条件付き期待値 (stressed pips/trade) と
  分布シフトの再現性**。的中率52%で期待値正のfamilyは、的中率70%で期待値負のfamilyに勝る。

## W25 プロトレーダーの脳 (2026-07-18 オペレーター要求「情報を集め勘でトレードする脳」)

勘 = 経験で圧縮されたパターン認識 × 自分の読みの信頼度の自覚。4層で実装する:

| 層 | 人間のプロ | 機械の実装 | 状態 |
|---|---|---|---|
| 1. 知覚 (情報の広角) | チャート・ニュース・COT・板・セッションを同時に見る | evidence packet (28ペアMTF+news+calendar) + W23独自指標 (spread状態/通貨strength/メタモメンタム/先例一致) | packet稼働中、独自指標はM5後 |
| 2. 直感 (統合と読み) | 「なんか嫌だ」「これは伸びる」 | **LLM market-read そのもの** (予測ファースト原則)。narrative→thesis→予測を毎サイクル言語化して ledger に記録 | `market_read_predictions.jsonl` 稼働中。7/16以降 GO/CAUTION/STOP に縮小されているのが gap |
| 3. メタ認知 (読みの信頼度) | 「今日は自分の読みが当たる日か」 | W17監督採点 + 予測スコアの regime別分解。**当たっていない状態の自覚がサイズを絞る** | scorer実装済み、運転はライブ再開後 |
| 4. 規律 (勘の暴走防止) | 損切り・サイズ・休む勇気 | ladder/ガード群/棚卸し (今夜実装済みの全部品) | 実装済み |

要点: **直感エンジンは既に存在する** (ルーチンのClaude trader = 予測ファースト)。7/16 cutover で
層2が最小化されたのが「脳が無い」ように見える原因。月曜の案Aは層2の復帰であり、
今夜の層4部品がその暴走防止。勘は「学習された読み」なので、層2は必ず層3の採点と対で運転する
(採点なしの勘はギャンブル)。層1の広角化 (W23) と層3の regime別採点が Codex 実装対象。

### 実装済み: 合成脳サイクル (`shadow_trading_brain.py`)

16のシャドウ部品を1本の fail-closed サイクルに合成した。層構造をコードで表現:
- 層1 知覚: evidence packet + 独自指標の sha 束縛
- 層2 読み: **`CODEX_AI_TRADER` 専用の read handoff 契約**。脳は読みを検証・消費するが著述しない。
  `SHADOW_PLACEHOLDER` のみ自己生成可で、その旨をフラグ化。オペレーター指示「直感はCodex AIトレーダー」を型で強制。
- 層3 メタ認知: 監督採点で不信頼familyを自動CAUTION降格
- 層4 規律: 通貨エクスポージャ上限 + conviction ladder サイジング
- seal: throughput SLO ファネル、GO risk 0、order_intents 空、authority NONE 不変
棚卸し UNRECONCILED は全候補を fail-closed。**live 発注は依然オペレーター承認 + Codex AI トレーダーの領分**。

## W26 全天候スパイン + 脳の完成度ロードマップ (2026-07-18, 独立監査で確定)

合成脳を独立監査 (4レンズ×敵対検証+完成度批評, 11 agents) にかけ、2件の fail-open を修理:
- 候補の `nav_exposure_fraction` 欠落/0 が通貨capを素通り → 正の値必須で fail-closed 化。
- 候補の `family_id` 欠落で監督 auto-CAUTION を回避 → family_id 必須 (`UNKNOWN_FAMILY`)。

**全天候スパインを配線** (measured>declared):
- `regime_classifier_shadow.py`: closed candle のみから ADX的効率比/BB幅/実現vol百分位で
  {TREND/RANGE/SQUEEZE/EVENT × LOW/HIGH} を**測定**。「時計でなく状態を測る」の実装。
- `regime_family_router.py`: regime×vol の8セルにfamilyを割当。未カバーセルを `UNCOVERED_CELL` と
  正直に露出 (現状8セル中5セル未カバー、range×highを含む)。
- 脳は measured cell に eligible な family のみ入場許可。read の declared regime が測定と矛盾したら flag。

**完成度批評5件の進捗**:
1. ~~regime分類器~~ **実装済み** (`regime_classifier_shadow.py`, scale-free/相対閾値)。
2. ~~ポートフォリオ共分散リスクモデル~~ **実装済み** (`portfolio_covariance_shadow.py`, Meucci ENB + Jacobi)。
3. ~~合成脳walk-forwardアトリビューション~~ **実装済み** (`run-all-weather-attribution.py`, W27セル別P&L)。
4. ~~conviction接地+較正層~~ **機構実装済み** (`conviction_calibration_shadow.py`): 独立再計算による
   condition接地 (過信の読みを減点)、Brier/ECE較正、grounded conviction×regime→実現期待値の
   連続0-1確信度乗数。**較正テーブル自体はライブ read/outcome ledger 待ち** (Codex,月曜以降)。
   W25核心「勘=パターン認識×信頼度の自覚」の機構化。5件全て機構完成。
5. ~~レンジ/平均回帰 shadow family (レーンF)~~ **機構実装済み** (`range_rail_shadow.py`, 受動LIMIT→mid回帰)。
   統計的proof は M5 corpus 待ち。W28が証明した「加算」の本体。

## W27 全天候アトリビューション (2026-07-18, 実測)

合成脳のwalk-forwardアトリビューション (完成度#3) を survivor で実行し、各取引の regime×vol を
決定時点で測定 (closed candle のみ) して P&L を帰属。**重要な副産物**: アトリビューションが
分類器の較正バグ (絶対BB幅閾値0.0025が全窓をSQUEEZE判定) を実データで検出 → 全境界を
scale-free (効率比0-1) / 相対 (自己履歴の百分位) に修正。時刻ヒューリスティック禁止と同じ教訓。

セル別損益 (TRAIN+VAL 656取引、correctness修理後のclassifierで再計算):

| cell | trades | net pips | mean |
|---|---|---|---|
| SQUEEZE_LOW | 126 | **+1096.7** | +8.70 |
| RANGE_LOW | 185 | +449.7 | +2.43 |
| TREND_HIGH | 55 | **+412.5** | +7.50 |
| SQUEEZE_HIGH | 15 | +185.9 | +12.39 |
| RANGE_HIGH | 172 | −21.2 | −0.12 |
| TREND_LOW | 103 | **−64.9** | −0.63 |

読み: エッジは**スクイーズ・ブレイクアウト + 強トレンド高vol**に集中。**弱トレンド低vol (騙し) と
荒れたレンジ高vol で出血** = 負の日の主因。=> survivor は {SQUEEZE_*, TREND_HIGH, RANGE_LOW} 専任、
{TREND_LOW, RANGE_HIGH} は router で CAUTION、後者はレーンF (レンジ) が埋めるべき空セル。
**注意**: セル判定はTRAIN+VAL in-sample。cell-gate採用には TRAIN選抜/VAL複製/未来窓の規律必須
(時刻R1と同じ overfitting 罠を避ける)。

## W28 セルゲーティング実測: 減算 vs 加算の証明 (2026-07-18)

TRAIN選抜/VAL複製の規律で負けセル (RANGE_HIGH, TREND_LOW) を除外 (correctness修理後classifier):
- VAL 負の日 **6→3 (半減、外部窓で初めて負け日数を減らせた介入)**。TRAIN/VAL両方で一貫=蜃気楼でない実効果。
- ただし VAL net **1251→726 (-42%)**。負けセルにも勝ち玉が居るため、減算は必ず net を払う (修理後さらに顕著)。

**理論的帰結 (全天候の核心)**: 負の日を**減算 (セル/時刻カット)** で消すと必ず net を失う。
負けセルを**正**にする唯一の道は、その天候で実際に勝つ**別family=加算**。
減算は分散を下げ利益も下げる / 加算のみが両方を改善する。=> レーンF (レンジ) 等の
family加算が唯一の全天候経路であることが実験で確定。オペレーターの「全天候勝てる」は
1エンジンでは不可能、複数family必須、が数理的に証明された。

## W29 P(3倍月) の実測と「3倍システム」の定義 (2026-07-18)

オペレーター目標「3倍になるシステム」を測定可能量として定義: **月倍率は分布であり、
設計目標は P(3倍月) の最大化 (生存制約付き)**。2025年の4倍も正期待値×集中サイジング分布の右尾実現。

現在のスタック (survivor+50pストップ) の実測分布 (週ブロックbootstrap 1万ヶ月, sealed):

| L | P(≥3x) | P(≥2x) | median | P(損失月) | P(DD>30%) | p99 |
|---|---|---|---|---|---|---|
| 10x | 0.0% | 0.0% | 1.13x | 19.6% | 0.0% | 1.55x |
| 25x | **0.6%** | 11.7% | 1.32x | 22.8% | 18.3% | 2.88x |

**レーン加算第1弾 (素朴レンジレール60分HL) は棄却**: 2,649 fill / -5,520.9p / 34日全敗、
平均-2.08p/fill=スプレッド負け+レール接触の逆選択 (W8の警告通り)。合算1.24x→0.59x。
教訓: 加算は「エッジのあるレーン」のみ。エッジ無しの加算は減算より悪い。
レーンF本体はM5+WRCの正式研究必須、近道なし (死因コード封印 `range_rail_naive_rejection_v1.json`)。

P(3倍)を上げる3レバー (優先順):
1. **エッジのあるレーン加算** — M5取得→8通貨strength/レーンF正式研究 (最大レバー)
2. **確信度集中** — 較正テーブル (ライブ read/outcome 蓄積後)。機構は実装済み
3. **履歴拡張** — 単一レジーム34日の分布は不確か。M5で分布自体を再推定

## W30 スイープ最終結果と月曜スタック確定 (2026-07-18)

事前宣言8本スイープ + ナンピン3段検死 (per-unit→悲観同時性→実同時性+ランタイム強制) の結論:

**採用スタック**: survivor (因果週末ゲート) + 50p日中ストップ + 実行可能サイジング (下記修正)
+ ランタイム証拠金上限92% (約定時強制、履歴較正禁止) + 3ゲート + conviction ladder。
実測実力: 未証明額面で月~1.5x、P(3倍月)=0.6%。

**不採用 (全て外部窓かカネ勘定で反証)**: 日スキップ/時刻/score床/コスト比/score比例/
トレーリング30p/ブレイクイーブン/半分利確/24h延長/ピラミッド/素朴レンジレール/ナンピン(成長用途)。
ナンピンは worst day を一貫して浅くする (VAL -8.1→-6.0%) ため「ドローダウン整形」に分類、
将来 T1 で再評価可。

**baseline実行可能性の修正 (重要)**: 25x/12設計は entry遅延で実ピーク14ユニットに達し
瞬間112%使用=執行不能。実行可能な基準は **~21.4x (VAL~1.55x)**。以後の全比較はこの基準。

オペレーターの証拠金許可 (92-95%) の運用形: 常時使用ではなく**ランタイム強制の上限**として
実装 (残余力が無い注文を約定時に拒否)。1点集中95%は26pipsでロスカットのため不可のまま。

## W31 ボット本体・盤面リーダー・データ枯渇宣言 (2026-07-18 最終)

**ボット本体を実行可能形で構築**: `run-shadow-bot-cycle.py` が1コマンドで
盤面読み→レジーム測定→ルーティング→合成脳→封印サイクルを完走 (demo実証済み)。
設定は調整契約のみから注入。Codexの残作業 = live feed接続と発注ゲートウェイ配線のみ。

**盤面リーダー** (`market_conditions_reader.py`): オペレーター指摘「市況を読め」の実装。
per-pairでなく盤面全体 — トレンド広がり率、支配regime、高vol比率、**通貨テーマ検出**
(広がり加重: 4ペアで確認されたUSDは1ペアの大きな動きに勝つ)、盤面判定
(THEMED_TREND/BROAD_TREND/COMPRESSED/MIXED_RANGE/EVENT)。層2のread前処理・router・
戦術切替に供給。

**バッチ2/3 判定**: 状態条件付き戦術 (市況ナンピン/勢い刈り取り/ペア選抜/セル傾斜) は
**全てTRAIN選抜規則を通過せず**。ただし市況ナンピンはVAL側で 1.512→1.602x + 負日6→4 の
一貫した芽 (24h延長・セル傾斜と同型のTRAIN/VAL緊張)。ペア選抜はTRAIN+114%→VAL崩壊の蜃気楼。

**データ枯渇宣言**: 34日S5は20の宣言テストで搾り尽くした。以後の全ノブがTRAIN/VAL矛盾を
出す = このデータから引き出せる知識の終端。次の情報は 8/3未来窓・M5コーパス・ライブ蓄積のみ。
5月(TRAIN)と6月(VAL)のレジーム構成差が全矛盾の根 — M5の複数年データだけが
「どちらが常態か」を解ける。VAL側で繰り返し芽が出る状態条件付き戦術群は
M5研究の最優先仮説として登録。

## 実行順 (Codex)

P0: W16 (T1契約モジュール=Claude実装済みを検収) → W8+W2 (regime×order-type整合) → W4 (event gate) → W12 (通貨エクスポージャ制約)
P1: W1+W9 (V3出口family) → W6 (range-railレーンF) → W13 (NAV%化) → W20 (並列登録簿) → W3 (touch-reject admission)
P2: W5 (session次元) → W14 (equity条項) → W17 (監督採点) → W18 (死因taxonomy) → W19 (gate-debt review) → W10 (時間帯マスク)

検収の共通原則: 各修理は「殺すエントリー数」と「守る損失」の見積りを添付し、
gate throughput SLO (S2) の floor を割る修理は設計に差し戻す。

## W32 M5コーパス取得・封印完了 (2026-07-18 昼)

オペレーター指示「検証期間を広げろ」を実行。パイロット (EUR_USD 2020) で `to`境界の懸念を
実測解消 (排他確認・validator実データ初合格・カバレッジ70.8%=理論値) → 本取得。

- **28ペア × 2020-01-01〜2026-07-10、7年次シャード、196セル、約1,360万行、331MB、エラー0**
- 封印: `exact28_m5_history_manifest_2020_2026.json` (sha `de981dfb…`)、fail-closed全検証 VERIFIED
- root: `QuantRabbit-live/logs/replay/oanda_history_m5_2020_2026` (S5封印rootとは分離)
- ホールドアウト規律継続: 2026-07-10以降は未取得。8/3の未来テストは別途新規取得。

### M5開幕研究バッチ (宣言済み、次セッション/Codex)

1. 未決仮説の裁定: 市況ナンピン / レジーム条件付き保有 / セル傾斜 / ストップ解除3変種
   (5月型vs6月型の対立を全レジームで決着)
2. レーンF正式研究 (レンジ収益の本体) + 8通貨strength分解12候補
3. 会計は ¥200,000基準の金額標準・年単位TRAIN(2020-23)/VAL(2024-25)/TEST(2026H1)・
   week block bootstrap・White Reality Check。統計proofなしの採用は不可。

## W33 M5開幕戦の判定: 通貨strengthモメンタムは前提から棄却 (2026-07-18 午後)

12固定候補 (lookback 8/24/120h × hold 4/8h × TOP1/TOP2)、TRAIN 2020-23、¥200k・10x金額会計:
- **ネット: 12/12全損 (-100%)** — 常時回転×広スプレッドクロスの製粉機 (naiveレンジレールと同じ死因)。
- **グロス (mid-to-mid): 12/12マイナス (最良-58.5%/4年、WRC p=0.81)** — コスト以前にエッジが無い。
  **通貨strengthモメンタム4-8h保有は2020-24年に存在しない**。引継ぎ書の本命方向を前提レベルで反証。
- 逆張り側のグロス+58.5%/4年 (月~1%) はスプレッドで即死する薄さ。VALDATION/TEST未開封のまま棄却。
- 教訓の三重証明: 執行コストは戦略設計の第一級制約。高回転設計は原則死。
- 8/3のS5 survivor判定への含意: 断面モメンタム族は壊れやすい — 期待値を上げすぎないこと。

**M5時代の生き残り研究方向 (次family宣言候補)**: ①閾値ゲート型・低頻度 (エントリー条件が
揃った時だけ、majors限定でスプレッド最小化) ②レーンE (オペレーター2025先例の状況タプル)
③W23相互作用特徴 ④レーンF (レール事前確定型、ただし閾値・majors設計で)。
いずれも「トレード数を桁で減らして1取引あたりのエッジを跳ね上げる」設計のみ許可。

## W35 裁量エッジ定理 (2026-07-18 セッション最終結論)

レーンE1 (オペレーターの2025年プロファイルの機械化: USDJPY/ロンドン朝/トレンド側/4barマイクロブレイク/
TP10-SL7/2h) を単一候補・選抜なしで6年判定:
- **地上真実照合: 同一期間・同一ペアでオペレーター +266,816円 vs 機械化ルール -56,624円**
- TRAIN 2020-23 / VAL 2024 / 2025 全分割マイナス。死因コード封印。

**定理**: 今夜の20+のルール族の全滅と、この地上真実照合により — オペレーターのエッジは
条件 (セッション/ペア/方向/保有) ではなく**リアルタイムの裁量判断そのもの**に宿る。
固定ルールへの蒸留は不可能 (外形を完全コピーしても負ける)。

**帰結 (毎月3倍構造の最終形)**:
1. エッジ生成器 = 層2の裁量読み (CODEX_AI_TRADER) — オペレーターの配役指定がデータで正当化された。
2. 機械の役割 = 規律装置 (知覚/ゲート/サイジング/棚卸し/採点/コスト管理) — 今夜の全部品。
3. 3x構造 = 裁量読み × 規律の檻 × 採点較正 (W25脳)。オペレーター実績 +266,816円/2ヶ月が
   このアーキテクチャの実在証明。機械単独のシグナル生成は全周波数帯で反証済み。
4. 次の実行: 月曜の案A (ルーチン=裁量トレーダー再開、新ゲート装備) + read採点の蓄積 +
   較正テーブル構築。8/3のS5判定は形式上実行するが期待値は低い。

## W36 再チューニング・ループ検証 (2026-07-18 最終補遺)

オペレーター指摘「AIが定期的にボットをチューニングしながらやる — 考慮したか?」への回答実験。
walk-forward再チューニング (8道具+FLAT、毎週月曜、直近28日成績で因果的に再選抜、142回切替):
- **監督ループも全滅** (6年NAV≈0、全年-67〜-95%)。凍結8道具も全て≈0。
- **解釈: 前提の反証ではなく条件の確定** — 切替ループは道具箱に「コスト後ネット正エッジを
  どこかの状態で持つ道具」が1つ以上あるときだけ機能する。今夜の全探索でその道具はゼロ。
- 3アーキテクチャ検証完了: 凍結ルール=死 / ルール+定期再チューニング=死 (道具にエッジ無き限り) /
  取引毎の裁量=唯一の実証プラス。
- **AIチューニングの正しい適用先 (確定)**: ①裁量レーンの監督 (read採点/確信度配分/GO-CAUTION-STOP)
  ②規律パラメータの市況調整 (ストップ/解除/上限) ③新道具の探索ループ (要件: 低頻度・状態限定・
  コスト後プラス)。= 7/16カットオーバーの設計思想がデータで正当化された。
- 最終形: **裁量の読み (エッジ) × AI定期チューニング (監督・規律・配分) × 規律の檻 (機械)**。

## W37 グリッド在庫問題の解決 + 盲検AI読み (2026-07-18 夕)

**オペレーター質問「ロング/ショートが溜まって証拠金死 — 解決策は?」→ 解決策あり (実証)**:
- 保護3点 (在庫ハードキャップ5段 / レジームkill-switch / 再稼働使用率60%上限) で
  6.5年間死亡ゼロ (保護なしは2025/2に証拠金死、残高¥1,137)。
- **+10%収穫日は71日と維持** (オペレーターの記憶「数時間で10%」は実在、6.5年で62-71日)。
- 2025年は保護つき+274% = オペレーターの黄金期記憶と一致。
- ただし通算は-80%: 生存≠利益。残課題は安全でなく**選別器** (2020/22/25のfeast年と
  2021/23/24のfamine年を事前に分ける状態信号)。RANGE/SQUEEZE測定では2024 (-93%) を避けられず。

**盲検AI読みパイロット** (匿名化40場面・4独立読者・封印答え合わせ):
- 的中41.2% (p=0.885)、コスト後-2.5bp/取引、確信度と成績無相関。
- **チャート構造のみのAI裁量にエッジ無し**。実録read (フル文脈) はn=14で判定不能。
- 帰結: AI裁量の実証経路は「月曜からのフル文脈read蓄積 × W17採点 × 極小サイズ運転」のみ。

## W38 MomentumBurst / pullback_s5 の救済裁定 (2026-07-18 夜)

**オペレーター記憶の正体特定** (旧アーカイブ台帳 2025-10-22〜2026-02-06):
- 「IMPULSみたいな名前」= ImpulseRetrace (76取引 -4,856円、負け) と MomentumBurst の混同。
- 台帳最強のワーカー単日 = **MomentumBurst 2025-12-09: +24,542円 53戦53勝** (USD_JPY
  トレンド日の順張りバースト+3pips即利確)。翌朝、出口規律なしの3本が-14,461円 (59%返上)。
- 「上下に在庫→マージン死」の台帳標本 = **pullback_s5 2025-11-05**: 340取引 (L149/S191)、
  回転329本はネット-41円=トントン、取り残されショート15本の強制決済-37,620円が損失の99.9%。
- 真の夏の10%日 (2025年7月、残高73万→206万) は台帳開始前で明細なし。

**M5機械化裁定 (momentum_burst_replica_v1, 3アーム宣言済み)**:
- RAW (無保護·無限積み増し): 181回強制決済、2020-07-16死亡。オペレーター記述の死を機械的に再現。
- PROTECTED (在庫3·60分タイムストップ·トレンド反転フラッシュ): 死亡ゼロ — **死は消せる (3例目)**。
  だが61,470取引のスプレッドミルで残高→0。
- PROTECTED+TREND限定稼働 (実測レジームゲート): それでも残高→0。**素朴なM5バースト機械化は
  レジーム選別でも救えない** (W33 M5モメンタム全滅と整合)。
- 帰結: **保護は死を消すが、勝ちはM1ゲートスタック (1,376行) の中にしか居ない**。履歴では再現
  不能 (M1データなし+W35機械化定理)。救済経路 = アーカイブの実ワーカーコード+保護3点を
  shadow復活させ prospective registry で前向き採点するのみ。pullback_s5 はさらに
  LIMIT受動化 (スプレッド払う側→貰う側) が前提条件。

## W39 目・手・整備士アーキテクチャの裁定 + 出口盲検 + スプレッド表 (2026-07-19未明)

**eye-hand sim (2ワーカー×3つの目, 6.5年, 金額会計)**: ALWAYS_ON x0.019 / MEASURED(昨日クラス持続) x0.020 /
**ORACLE(完璧な天気予報, 非因果上限) でも TRAIN x0.091・VAL x0.928 = 負け**。
→ **定理: 手が負の期待値なら、目が完璧でも救えない。優先順位は 手 > 目**。目の価値は出血半減
(worst -12.5%→-8.9%, 月率x0.53→x0.91) までで、符号は手のコスト後エッジだけが決める。
W36 (道具にエッジ無ければ監督ループ無効) の合成版として整合。

**出口盲検パイロット (40局面×4読者, blind_exit_pilot_v1)**: 機械タイムストップ -8.15 / 常時ホールド -7.53 /
読者 -7.38〜-7.70 (正解率60-65%) / オラクル -2.91 pips/建玉。
→ **AI出口裁量は鈍器タイムストップに+0.6pips勝つ (構造のみでも)**。ただしオラクル余地の~12%しか
取れておらず、残り4.6pipsがフル文脈在庫裁量の賞金。在庫管理AI裁量は「機械より良い」が実証、
「十分良い」は前向き採点待ち。3層構造 (AI裁量の在庫判断 + 機械ハードキャップ + デッドマンスイッチ) を採用。

**スプレッド実測 (corpus 2024-25, ロンドン+NY時間median)**: AUD_USD/EUR_GBP 1.3p, NZD_USD 1.4p,
EUR_USD/USD_CHF 1.5p, USD_JPY 1.6p, GBP_USD 1.8p, AUD_JPY 2.0p, EUR_JPY 2.2p, GBP_JPY 3.1p。
→ ミル型ワーカーは最安ペア専用 + LIMIT受動 (払う→貰う) が生存条件。多通貨化はfeast日供給と
ENB分散を増やす (W28加算原則と整合)。corpusスプレッドはライブより広い保守値である旨注記。

## W40 武器庫ランタイム化 (2026-07-19)

`worker_arsenal.py` (16テスト): 目=層2がセル宣言→ワーカーarm/disarm、AI在庫裁量 (HOLD/CUT) +
機械タイムストップfloor + 4xハード天井 (12/9型宿泊取り残し防止) + デッドマンスイッチ (heartbeat 20分
TTL、6/9無音36h事件対策)、bleedセル/UNCLEAR/EVENTは宣言的に永久disarm、live昇格は
operator承認sha+前向き証明shaの二重束縛。武器庫契約 v1 (`worker_arsenal_contract_v1.json`) は
5ワーカー (W_COMPRESSION/W_GRID/W_BURST/W_SPIKE_FADE/W_PULLBACK) を8セルに配置し、
今日の5実験shaに束縛。全員SHADOW発進 — 手のコスト後プラス証明が唯一の昇格経路。

## W41 黄金の手の最終審判 — 本物のコードでも死んでいた (2026-07-19)

**M1史料取得**: 派生fetcher (削除ガード準拠、凍結原本不変) で USD_JPY/EUR_USD M1 BA 2020〜2026、
480万行・全14シャードエラー0。**忠実リプレイ**: 12/9当日のHEAD commit 41644097 から逐語ベンダリング
した MomentumBurstMicro.check を無改変で判定器に使用。当日式の因子 (_rsi/_atr/_adx/MA/EMA/vol_5m)。

**忠実性の実証**: 実corpusスプレッドでは12/9シグナル1個だが、`spread_pips=0` (ライブのspread monitor
不在挙動) で **58個 ≒ ライブ53取引**。→ ライブ機はスプレッドゲート実質OFFで走っていた。
GATE_OFF_AS_LIVED を参加モードとして採用 (執行コストは全アームで実bid/ask全額課金)。

**審判**: ①GATE_OFF (ライブ忠実参加): 勝率14%、保護有無に関わらず **2021年夏に資金全滅**
(margin死でなくSL千本切り。保護は無力 — 死因が在庫でないため)。②GATE_ON (ゲート強制):
TRAIN -85% / VAL -12% / 2026H1 -0.4%、全窓マイナス。
→ **黄金の手定理: 12/9に53連勝した本物のコードそのものが、実コスト下の6.5年で全構成マイナス。
53/53はレジーム運と本物のコードで確定**。「本物ならエッジがあるかも」仮説は死亡。
歴史裁定はこれで完結: 凍結機械 (簡易・本物とも) 全滅 / 再チューニング無効 / オラクル選別も無力。
残る利益経路 = ①新しい手の前向きshadow実証 (武器庫は採点準備済み) ②裁量レーン。
感度注記: corpus M1-close スプレッド中央値1.5-1.9pは本口座の価格ストリーム実測値。

## W42 コスト感度の閉鎖 — スプレッドのせいでもなかった (2026-07-19)

オペレーター指摘「意図的に作っていないのでは」を受けた最終感度検証。
**実効コスト実測**: 12/9のライブ約定53本 vs corpus中値 → 実効スプレッド median ~1.0p (片道~0.5p)。
corpus BA気配 (1.5-1.9p) は実コストを~40%過大評価していた — 正当な疑義だった。
**再審判 (mid±0.5p、実測実効コスト)**: それでも 12,620取引・勝率18.7%・**2022年1月に資金全滅**
(全コスト時より約6ヶ月延命したのみ)。→ 死因はスプレッドでなく **SL0.9-2p/TP1.3-3p幾何そのもの**
(勝率19%ではどんなコストでも負)。12/9はSLに届く押し目が無かった日。
**歴史裁定はこれで完全終了**: 本物のコード×実測コストでも死。環境=live (api-fxtrade) 確認済み。
残る建設経路は変わらず ①武器庫の前向きshadow実証 ②裁量レーン。ここに嘘の裏道は作らない。

## W43 実市場シャドー環境 (2026-07-19)

オペレーター指示「実際の市場と同じ環境。時間もコストも偽りなし」を実装:
`run-live-shadow-environment.py` — 壁時計時間・本口座ライブ気配 (読み取り専用)・シャドー約定は
実気配タッチ時のみ・hash-chain台帳・stale/closed拒否・発注権限なし。
出口幾何二重簿記 (BOOK_SL=戦略SL付き / BOOK_TPO=TPのみ+4h天井) で、歴史リプレイが再現し切れなかった
「ライブはタイトSLを付けていなかった」問題をライブデータが直接裁く。
smoke実証: 市場閉鎖中はMARKET_CLOSEDを刻んで取引拒否 (偽りなし動作)。気配疎通確認済み
(金曜クローズ気配・スプレッド3.8p拡大状態・90s stale拒否が有効)。
**監査記録 (W42追補)**: リプレイTPハンデバグ (SL=bar+1有効/TP=bar+2) を修正。両タッチ曖昧率1.3%、
悲観/楽観ブラケット僅差 (勝率20.1%/21.6%、どちらも2022年死) — 6.5年負け判定はブラケット両側で頑健。
ただし12月検算: 12/9再現58取引14勝 vs ライブ53勝 → **ライブは戦略の出すタイトSLを付けていなかった**
(当日53件全部TP決済)。出口幾何の歴史再現には限界 → 実市場環境が最終裁定者。

## W44 仮想マーケット環境 (2026-07-19)

オペレーター指示「現実と同じ仮想環境でトレードしまくりたい。裁量の再現は担当エージェント」実装:
`virtual_broker.py` (13テスト、OANDAメカニクス完全再現・実気配約定のみ・hash-chain・発注経路なし) +
`run-virtual-market-session.py` (live/replay両フィード、ファイル発注プロトコル、--stepターン制)。
smoke実証: 12/9史実で成行+指値→実気配タッチ約定→TP→+¥1,000。2026年データはカットオフ後で
裁量練習の後知恵汚染が最小。docs/virtual_market_environment.md が担当エージェントの操作書。

## W45 DOJO命名・監査・汎用化 (2026-07-19)

仮想環境の正式名称 = **DOJO (道場)**。敵対的監査で5穴を発見・修理: ①バー内OHLC順のロング楽観
バイアス (→--intrabar両側ブラケット) ②ライブモードのボット未発火 (→ライブM1組み立て追加)
③inbox書き込み競合の注文消失 (→0.5s猶予) ④全史リプレイのメモリ爆発 (→年ストリーミング)
⑤ボットのハードコード (→ --bot-module プラグインローダ)。
**任意エージェント/任意ボット対応を実証**: 外部ファイルの新作ボット (bots/example_bot.py) が
DOJO無改修で29取引実行。OLHC悲観側でも12/9結果不変 (両タッチ曖昧なし)。全29テストpass。
残存制約は docs/virtual_market_environment.md に宣言 (バー内合成経路/5秒ポーリング極値/換算/滑り)。

## W46 道場の勝ち残り第1号 — W_FADE (2026-07-19)

宣言済み12構成グリッド (3シグナル×4出口、多重性全記録) を DOJO で TRAIN18ヶ月→未使用VAL12ヶ月→
OLHC悲観ブラケット→S5 12倍細粒度約定→S5全55日、の全関門に通した結果:
**W_FADE (レンジ両面LIMITフェード・効率比ゲート・SLなし・硬い天井) の2構成が全関門陽性**。
E1 (TP3/天井4h): TRAIN両側+26.6M/+27.4M、VAL +841k/+122k、S5 6月+13.7k、S5 55日+5.8k/+4.4k。
E2 (TP6/天井8h): TRAIN +481k/+507k、VAL +16.9k/+38.9k、S5 55日+30.6k。
**正直な実力帯 = S5解決値: E1 +0.8〜6.9%/月、E2 ~+4%/月** (M1約定値は楽観でありスコアとして引用禁止)。
中身はオペレーター哲学の機械化 (SL-free+レンジレール+状態測定ゲート+檻) — 本セッション全定理と整合。
残リスク: S5窓はVALと重複 / 5秒未満経路と実スリッページ未モデル / **最終証明は前向きのみ**
(DOJOライブペーパー→shadow採点→極小実弾昇段)。burst系は全出口で死亡継続 (歴史判定と整合)。

## W47 3倍への乗数の裁定 — 層A/B死亡、残るは層Cのみ (2026-07-19)

**層B (サイジング)**: 統一ルール掃引 (4.3-20x)。前線=lev16×1玉 (worst -27%)、lev20=破滅圏。
だが**S5実力帯はどのサイズ構成でも月+5-7%に収束** — 総エクスポージャ同水準なら玉の割り方は無関係、
サイズ比例で天井損も増える。層B乗数=×1。M1複利倍率 (x30-x115k) は粒度楽観の複利で全棄却。
**層A (多ペア)**: 7ペアS5 55日、pips固定TP=-97k、ATR正規化TP=-96k。**両方式でUSD_JPY以外の
6ペア全てマイナス、USD_JPYだけ両方式でプラス** → W_FADEのエッジはUSD_JPY固有 (オペレーターの
ホームペアと一致)。層A乗数=×1。修理: FILL_LIMITにpair記録追加、換算経路一般化、スプレッド/TP比ゲート。
**確定した3倍の方程式**: 機械の床=USD_JPY W_FADE 月+5-7% (死なない・複利)。残る唯一の乗数=**層C
(読み: feast日検出と確度集中)** — オペレーター自身の2.8倍月 (2025-07) が唯一の実在証拠。
DOJOターン制 (カットオフ後データ) とライブペーパーが層Cの練習場+採点器。

## W48 生き残り2号 W_SPIKE_FADE + 圧縮ブレイク棄却 + 口座事実 (2026-07-19)

口座実査: 68銘柄全FX (金・CFD無し — オペレーター指摘通り)。DOJOにSTOP注文実装
(自己バグ発見: 箱外LIMITは即約定 → STOPが正しい半約定意味論、テスト15本)。
**W_SPIKE_FADE (USD_JPY 2.5ATR超一本髭をLIMITで受ける・TP=3ATR・SLなし・8h天井) 全関門通過**:
TRAIN x2.05 / VAL両側 +91.7k/+126.1k (月+3-4%) / S5両側 +6.0k (月+2%)。
compression_break は TRAIN -193k で棄却 (SQUEEZEセルのM1機械化は不成立)。
**床の現在値: W_FADE (+5-7%/月) + W_SPIKE_FADE (+2-4%/月)、両方USD_JPY・SL-free・檻付き**。
相関と合算はDOJO同居運転で次に測る。クロス円M1取得進行中 (JPYクォート仮説)。
