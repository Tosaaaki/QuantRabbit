# 週次運用ドキュメント（2026-01-19〜2026-01-25 / JST）

## 目的
- 取り残しを早期に判断し、損切り→再エントリーで期待値を維持する
- factor cache stale ガードの効き過ぎ/不足を検証し、鮮度不足の誤発注を抑制する
- ワーカー別の差異分析（MFE/MAE/BE 到達時間）を可視化して基準を確定する

## 今週の重点タスク
- T-20260118-001: factor cache stale 監視と閾値調整（市場オープン後）
- T-20260118-002: ワーカー別 return-wait 確定と再エントリー条件更新
- T-20260118-003: entry_thesis フラグ保存 + MFE/MAE/BE 時間分析

## 市場オープン後チェック（最初の 24h）
- `factor_stale` が出ていないか（orders.db / log）
- `close_reject_no_negative` が再発していないか（orders.db）
- `FACTOR_CACHE` の stale → reseed ログ有無

確認コマンド（VM）:
```bash
scripts/vm.sh -p quantrabbit -z asia-northeast1-a -m fx-trader-vm -t \
  sql -f /home/tossaki/QuantRabbit/logs/orders.db \
  -q "select ts,status,client_order_id from orders where status='factor_stale' order by ts desc limit 5;"

scripts/vm.sh -p quantrabbit -z asia-northeast1-a -m fx-trader-vm -t \
  sql -f /home/tossaki/QuantRabbit/logs/orders.db \
  -q "select ts,status,client_order_id from orders where status='close_reject_no_negative' order by ts desc limit 5;"

scripts/vm.sh -p quantrabbit -z asia-northeast1-a -m fx-trader-vm -t \
  exec -- "journalctl -u quantrabbit.service -n 200 --no-pager | grep -E 'FACTOR_CACHE|factor_stale' || true"
```

## しきい値調整（必要時のみ）
- `ENTRY_FACTOR_MAX_AGE_SEC` を短縮/延長
- `FACTOR_CACHE_STALE_SEC` を短縮/延長
- `FACTOR_CACHE_REFRESH_MIN_INTERVAL_SEC` を短縮/延長

## 差異分析のアウトプット
- ワーカー別: MFE/MAE 分布、BE 到達時間、戻り待ち有利/不利の最終判定
- 反映先: `config/worker_reentry.yaml`（cooldown / reentry pips / bias）
- 共有先: `docs/hedge_plan.md` と本ファイルに要点を集約

## 成果物
- 週次まとめ（本ファイルに追記）
- 変更が必要な場合は `docs/TASKS.md` の Notes/Status を更新
