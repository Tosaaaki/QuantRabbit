# Copy this file to scripts/vm.env and adjust to your environment.
# Do NOT commit scripts/vm.env (add it to .gitignore).

# Required
PROJECT=quantrabbit
ZONE=asia-northeast1-a
INSTANCE=fx-trader-vm

# Optional
#ACCOUNT=
KEYFILE=$HOME/.ssh/gcp_oslogin_quantrabbit
USE_IAP=1
REMOTE_DIR=/home/tossaki/QuantRabbit

# Convenience defaults for make targets
BRANCH=main
VM_SERVICE=quantrabbit.service
INSTALL=1
