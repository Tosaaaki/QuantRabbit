# Entry-frequency hotfix (2026-02-11)
# Goal: remove non-critical blockers that are suppressing live entries.

# Keep stale-factor bypass for fast pockets (already applied).
ENTRY_FACTOR_STALE_ALLOW_POCKETS=micro,scalp

# M1Scalper: do not hard-block counter-side by H1 trend.
M1SCALP_HTF_BLOCK_COUNTER=0

# Prefix-aware perf guard (workers/common/perf_guard.py uses PREFIX_* only).
M1SCALP_PERF_GUARD_ENABLED=0
SCALP_PRECISION_PERF_GUARD_ENABLED=0
